services:
  image-sync:
    build: .
    container_name: image-sync
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./tmp:/app/tmp
      - /etc/localtime:/etc/localtime:ro
    restart: unless-stopped


